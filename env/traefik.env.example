# Cloudflare credentials for DNS-01 challenge
# These are the standard, reliable names used by lego (Traefik's ACME client)

CF_API_EMAIL=your@email.here
CF_DNS_API_TOKEN=TWU24...your Cloudflare API token here (DNS:Edit)
